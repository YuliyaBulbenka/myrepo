from django.conf.urls import url
from search.views import VideoListView

#app_name = 'search'
#urlpatterns = [
    #url(r'^$', VideoListView.as_view(), name='index'),
    #url(r'^$', views.ESearchView.as_view(), name='index'),
#]